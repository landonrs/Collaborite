const assert = require('chai').assert;
var sinon = require('sinon');
var db = require('../db/collabDB');

describe("dbConnection", function() {
    it("should get test user from db", function(){
        assert.isTrue(true);
    });
});